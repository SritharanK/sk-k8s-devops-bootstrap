# -----------------------------------------------------------------------------
# Stage environment overrides for payments-example.
# Stage mirrors production configuration but targets stage-specific services.
# Use with an Argo CD Application that has destination.namespace: stage
# and valueFiles: [values-stage.yaml].
# -----------------------------------------------------------------------------

helm-common:
  name: payments-example
  tier: application
  image:
    name: "YOUR_REGISTRY/payments-example:PLACEHOLDER_TAG"  # Overridden by CD pipeline
    pullPolicy: IfNotPresent
    containerPort: 80
    imagePullSecrets: dockerhub-secret
  extraAnnotations:
    configmap.reloader.stakater.com/reload: ""
  env:
    - name: WORDPRESS_DB_HOST
      value: "db.stage.svc.cluster.local:3306"
    - name: WORDPRESS_DB_USER
      value: wp_admin
    - name: WORDPRESS_DB_PASSWORD
      value: "CHANGE_ME"
    - name: WORDPRESS_DB_NAME
      value: surge_stage
  replicas: 1
  livenessProbe:
    tcpSocket:
      port: 80
    initialDelaySeconds: 30
    periodSeconds: 10
    failureThreshold: 3
  readinessProbe:
    tcpSocket:
      port: 80
    initialDelaySeconds: 10
    periodSeconds: 5
    failureThreshold: 3
  resources:
    requests: { memory: "64Mi", cpu: "50m" }
    limits:   { memory: "256Mi", cpu: "300m" }
  nodeSelector:
    node-role.kubernetes.io/worker: "true"
  autoscaling:
    enabled: false
  service:
    type: ClusterIP
    port: 80
  enabledDeployment: true
